package com.example.recycleview_practice

object DataProvider {
     var dataList=ArrayList<model>()
    fun  getData():ArrayList<model>{

        dataList.add(model(1,R.drawable.img1, "This is food we want"))
        dataList.add(model(1,R.drawable.img2, "This is food we want"))
        dataList.add(model(1,R.drawable.img3, "This is food we want"))
        dataList.add(model(1,R.drawable.img4, "This is food we want"))
        dataList.add(model(1,R.drawable.img5, "This is food we want"))
        dataList.add(model(1,R.drawable.img6, "This is food we want"))
        dataList.add(model(1,R.drawable.img7, "This is food we want"))
        dataList.add(model(1,R.drawable.img8, "This is food we want"))
        dataList.add(model(1,R.drawable.img9, "This is food we want"))
        dataList.add(model(1,R.drawable.img10, "This is food we want"))
        dataList.add(model(1,R.drawable.img1, "This is food we want"))
        dataList.add(model(1,R.drawable.img2, "This is food we want"))
        dataList.add(model(1,R.drawable.img3, "This is food we want"))
        dataList.add(model(1,R.drawable.img4, "This is food we want"))
        dataList.add(model(1,R.drawable.img5, "This is food we want"))
        dataList.add(model(1,R.drawable.img6, "This is food we want"))
        dataList.add(model(1,R.drawable.img7, "This is food we want"))
        dataList.add(model(1,R.drawable.img8, "This is food we want"))
        dataList.add(model(1,R.drawable.img9, "This is food we want"))
        dataList.add(model(1,R.drawable.img10, "This is food we want"))
        return dataList
    }
}